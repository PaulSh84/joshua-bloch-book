package exceptions.favoruseofstandardexceptions;

public class Rules {

    /* Do not reuse Exception, RuntimeException, Throwable, or Error directly.*/

    /* Treat these classes as if they were abstract. */

    /* If an exception fits your needs, go ahead and use it,
        but only if the conditions under which you would throw it are consistent with the exception’s documentation:
        reuse must be based on documented semantics, not just on name.
    */

    /* Subclass a standard exception if you want to add more detail */


}

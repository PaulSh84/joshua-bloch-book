package concurrency.synchronizemutabledata;

public class Rules {

    /* Synchronization is required for reliable communication between threads
        as well as for mutual exclusion.
     */

    /* Do not use Thread.stop.
     */

    /* Synchronization is not guaranteed to work unless both
        read and write operations are synchronized.
     */

    /* confine mutable data to a single thread.

     */
}
